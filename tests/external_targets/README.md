# External test targets

Ordinary `rules_python` targets in a standalone Bzlmod module. These fixtures test Pyrefly's
selection and mapping configuration for external labels without modelling any packaging ruleset.
